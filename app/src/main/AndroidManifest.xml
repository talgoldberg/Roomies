<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rommies">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/circled_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rommies">
        <activity android:name=".mytest"></activity>
        <activity android:name=".joinToExistApartment" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity
            android:name=".Register"
            android:label="@string/title_activity_register"
            android:theme="@style/Theme.Rommies.NoActionBar" />
        <activity android:name=".afterRegister" />
        <activity android:name=".Apartment" />
        <activity android:name=".MyAccount" />
    </application>

</manifest>